<%- include("./includes/head") %>
  <script src="/assets/js/index.js" defer></script>
  <title>Actions Note</title>
  </head>

  <body>
    <section class="c-container">
      <!-- MENU -->
      <%- include("./includes/menu") %>

        <!-- CONTENT -->
        <div class="c-content">
          <%- include("./includes/content") %>
          <% if(action == "register") { %>
          <form class="c-content__form" action="/note/form/register" method="POST">
            <% } else { %>
          <form class="c-content__form" action="/note/form/update?id=<%- action == 'edit' ? note._id : '' %>" method="POST">
          <% } %>
            <h2><%- action == "register" ? "Adicionar" : "Editar" %> nota</h2>
            <input type="text" name="title" placeholder="Titulo" value="<%- action == 'edit' ? note.title : '' %>">
            <textarea placeholder="Descrição" name="description" id="description" rows="10"><%- action == 'edit' ? note.description : '' %></textarea>
            <a href="/">Cancelar</a>
            <button type="submit">Salvar</button>
          </form>
        </div>
    </section>
  </body>

  </html>